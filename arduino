void setup()
{
  Serial.begin(9600);
  pinMode(13, OUTPUT);
}
void loop()
{
  //Serial.println("쉣더 뻑!");

  if(Serial.available()){
    char ch = Serial.read();
    digitalWrite(13, HIGH);
    
  } else {
    digitalWrite(13, LOW);
  }
  
  delay(50);
}
